spring:
  application:
    name: nacos-discovery
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.168.60:8848
        service: nacos-discovery
management:
  endpoints:
    web:
      exposure:
        include: '*'